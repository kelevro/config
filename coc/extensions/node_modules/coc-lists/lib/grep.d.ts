import { BasicList, ListContext, ListItem, ListTask, Neovim } from 'coc.nvim';
export default class GrepList extends BasicList {
    readonly interactive = true;
    readonly description = "grep text by rg or ag";
    readonly name = "grep";
    readonly defaultAction = "open";
    readonly detail = "Literal match is used by default.\nTo use interactive mode, add `-I` to LIST OPTIONS.\nTo change colors, checkout `man rg` or `man ag`\nGrep source provide some uniformed options to ease differences between rg and ag.\n";
    options: {
        name: string;
        description: string;
    }[];
    constructor(nvim: Neovim);
    loadItems(context: ListContext): Promise<ListTask | ListItem[]>;
}
