import { IList, ListAction, ListContext, ListItem, Neovim } from 'coc.nvim';
export default class Marks implements IList {
    private nvim;
    readonly name = "marks";
    readonly description = "marks list";
    readonly defaultAction = "jump";
    actions: ListAction[];
    constructor(nvim: Neovim);
    loadItems(_context: ListContext): Promise<ListItem[]>;
}
