import { IList, ListAction, ListContext, ListItem, Neovim } from 'coc.nvim';
export default class Searchhistory implements IList {
    private nvim;
    readonly name = "searchhistory";
    readonly description = "search history";
    readonly defaultAction = "feedkeys";
    actions: ListAction[];
    constructor(nvim: Neovim);
    loadItems(_context: ListContext): Promise<ListItem[]>;
}
