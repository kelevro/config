import { IList, ListContext, ListItem, Neovim, ListAction } from 'coc.nvim';
export default class Filetypes implements IList {
    private nvim;
    readonly name = "filetypes";
    readonly description = "available filetypes";
    readonly defaultAction = "set";
    actions: ListAction[];
    constructor(nvim: Neovim);
    loadItems(_context: ListContext): Promise<ListItem[]>;
}
