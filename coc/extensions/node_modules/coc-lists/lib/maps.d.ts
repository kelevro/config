import { IList, ListAction, ListContext, ListItem, Neovim } from 'coc.nvim';
export default class Maps implements IList {
    private nvim;
    readonly name = "maps";
    readonly description = "key mappings";
    readonly defaultAction = "open";
    actions: ListAction[];
    options: {
        name: string;
        description: string;
    }[];
    constructor(nvim: Neovim);
    loadItems(context: ListContext): Promise<ListItem[]>;
}
