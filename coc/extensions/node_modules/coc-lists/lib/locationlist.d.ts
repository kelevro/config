import { BasicList, ListContext, ListItem, Neovim } from 'coc.nvim';
export default class LocationList extends BasicList {
    readonly name = "locationlist";
    readonly defaultAction = "open";
    description: string;
    constructor(nvim: Neovim);
    loadItems(context: ListContext): Promise<ListItem[]>;
    doHighlight(): void;
}
