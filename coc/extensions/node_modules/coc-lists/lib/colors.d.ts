import { IList, ListAction, ListContext, ListItem, Neovim } from 'coc.nvim';
export default class Colors implements IList {
    private nvim;
    readonly name = "colors";
    readonly description = "color schemes";
    readonly defaultAction = "set";
    actions: ListAction[];
    constructor(nvim: Neovim);
    loadItems(_context: ListContext): Promise<ListItem[]>;
    doHighlight(): void;
}
