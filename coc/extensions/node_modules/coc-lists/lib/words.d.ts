import { BasicList, ListContext, ListItem, Neovim } from 'coc.nvim';
export default class Words extends BasicList {
    readonly name = "words";
    readonly searchHighlight = false;
    readonly interactive = true;
    readonly description = "word matches of current buffer";
    options: {
        name: string;
        description: string;
    }[];
    constructor(nvim: Neovim);
    loadItems(context: ListContext): Promise<ListItem[]>;
}
