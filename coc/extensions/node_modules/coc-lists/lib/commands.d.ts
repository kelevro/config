import { IList, ListAction, ListContext, ListItem, Neovim } from 'coc.nvim';
export default class Commands implements IList {
    private nvim;
    readonly name = "vimcommands";
    readonly description = "command list";
    readonly defaultAction = "execute";
    actions: ListAction[];
    constructor(nvim: Neovim);
    loadItems(_context: ListContext): Promise<ListItem[]>;
}
