export declare function executable(cmd: string): boolean;
export declare function characterIndex(content: string, byteIndex: number): number;
export declare function wait(ms: number): Promise<any>;
export declare function pad(n: string, total: number): string;
/**
 * Removes duplicates from the given array. The optional keyFn allows to specify
 * how elements are checked for equalness by returning a unique string for each.
 */
export declare function distinct<T>(array: T[], keyFn?: (t: T) => string): T[];
