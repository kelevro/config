import { BasicList, ListContext, ListTask, Neovim } from 'coc.nvim';
export default class FilesList extends BasicList {
    readonly name = "files";
    readonly defaultAction = "open";
    description: string;
    private excludePatterns;
    constructor(nvim: Neovim);
    private getArgs;
    getCommand(cwd: string): {
        cmd: string;
        args: string[];
    };
    loadItems(context: ListContext): Promise<ListTask>;
}
