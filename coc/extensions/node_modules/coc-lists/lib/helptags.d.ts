import { BasicList, ListContext, ListItem, Neovim } from 'coc.nvim';
export default class Helptags extends BasicList {
    readonly name = "helptags";
    readonly description = "helptags of vim";
    readonly defaultAction = "show";
    constructor(nvim: Neovim);
    loadItems(_context: ListContext): Promise<ListItem[]>;
    doHighlight(): void;
}
